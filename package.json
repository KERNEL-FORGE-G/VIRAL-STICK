{
  "scripts": {
    "start": "concurrently -n BE,FE -c cyan,magenta \"npm start --prefix backend\" \"npm start --prefix web\"",
    "build": "cd backend && npm run build && cd .. && cd web && npm install && CI=true npm run build && cd .. && rm -rf build && mv web/build build",
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^30.4.2",
    "prettier": "^3.8.4"
  },
  "dependencies": {
    "concurrently": "^10.0.3",
    "@google/generative-ai": "^0.1.3",
    "@mistralai/mistralai": "^2.2.6",
    "axios": "^1.18.1",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "firebase-admin": "^13.1.0",
    "cloudinary": "^2.5.1",
    "multer": "^1.4.5-lts.1",
    "openai": "^6.44.0",
    "sharp": "^0.33.5",
    "uuid": "^11.1.0"
  }
}
